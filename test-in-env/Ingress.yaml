apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: proxy-ingress
  namespace: test-env
spec:
  rules:
    - http:
        paths:
          - pathType: Prefix
            path: /proxy
            backend:
              service:
                name: proxy
                port:
                  number: 80
